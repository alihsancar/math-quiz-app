<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mathquiz">
        <activity
            android:name=".OnlineGameActivity"
            android:exported="false" />
        <activity
            android:name=".ClientActivity"
            android:exported="false" />
        <activity
            android:name=".HostActivity"
            android:exported="false" />
        <activity
            android:name=".OnlineMenuActivity"
            android:exported="false" /> <!-- SPLASH (LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- ANA MENÜ -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- OYUN -->
        <activity
            android:name=".GameActivity"
            android:exported="false" /> <!-- OYUNCU AYAR -->
        <activity
            android:name=".SetupPlayersActivity"
            android:exported="false" /> <!-- AYARLAR -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" /> <!-- GEÇMİŞ -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" /> <!-- SONUÇ -->
        <activity
            android:name=".ResultActivity"
            android:exported="false" />
    </application>

</manifest>